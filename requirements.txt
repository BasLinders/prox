pandas>=1.5.0,<4.0.0
numpy>=1.20.0,<3.0.0
matplotlib>=3.5.0,<4.0.0
seaborn>=0.11.0,<1.0.0
pm4py>=2.7.0,<3.0.0
streamlit>=1.61.0,<2.0.0
reportlab>=4.0.0,<6.0.0
db-dtypes==1.7.1
google-cloud-bigquery-storage

# Optional: BigQuery live data source (docs/dev_roadmap.md). Not installed
# by default - most users run PRoX CSV-only. Requires Python >=3.10 and
# pulls in first-order-engine's core deps (incl. Prophet, which may need a
# compiled Stan backend). Install (or reinstall to pick up upstream changes -
# foe's version string never bumps, so a plain `pip install` of the same URL
# silently keeps a stale build; --upgrade --force-reinstall makes pip
# actually re-clone) with:
#   pip install --upgrade --force-reinstall "foe[bigquery] @ git+https://github.com/BasLinders/first-order-engine.git"

# Optional: Predictive Insights / conversion propensity model
# (docs/ML_roadmap.md). Not installed by default - most users run PRoX
# without the ML layer. Adds scikit-learn (pure Python + numpy/scipy, no
# GPU/compiled deps). Install with:
#   pip install prox[ml]
